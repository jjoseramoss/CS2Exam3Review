#include <iostream>
using namespace std;

int factorial(int n) {
    // This function calculates the factorial of a given number n.
    // The factorial of n is the product of all positive integers <= n.
    // Example: factorial(5) = 5 * 4 * 3 * 2 * 1 = 120
}


int sum(int n) {
    // This function calculates the sum of natural numbers from 1 to n.
    // Example: sum(5) = 1 + 2 + 3 + 4 + 5 = 15
}


int fibonacci(int n) {
    // This function returns the nth Fibonacci number.
    // The Fibonacci sequence starts as 0, 1, 1, 2, 3, 5, 8, 13, ...
    // Example: fibonacci(5) = 5, because the sequence is 0, 1, 1, 2, 3, 5
}


int power(int base, int exponent) {
    // This function calculates base raised to the power of exponent.
    // Example: power(2, 3) = 2 * 2 * 2 = 8
}


string reverseString(string str) {
    // This function reverses the input string str recursively.
    // Example: reverseString("abc") = "cba"
}


void printNumbers(int n) {
    // This function prints all numbers from n to 1 recursively.
    // Example: printNumbers(5) will print 5 4 3 2 1
}


int countDigits(int n) {
    // This function counts the number of digits in the given number n.
    // Example: countDigits(123) = 3, because 123 has three digits.
}


bool isPalindrome(int n) {
    // This function checks if the given number n is a palindrome.
    // A palindrome reads the same backward as forward.
    // Example: isPalindrome(121) = true, isPalindrome(123) = false
}
